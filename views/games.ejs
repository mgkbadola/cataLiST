<div class="container">
    <input type="text" id="search1" class="search-box" placeholder="Search In Watched Games..."
           onkeyup="search('search1', '<%= header %>1')" title="Search for a game"/>
</div>
<br/>
<div class="container">
    <table id="<%= header %>1" class="table table-bordered">
        <colgroup>
            <col class="small"/>
            <col class="small"/>
            <col class="medium"/>
            <col class="medium"/>
        </colgroup>
        <thead>
        <tr style="background-color: #9147ff">
            <th>Name</th>
            <th>Year</th>
            <th>Platform(s)</th>
            <th>Genre(s)</th>
        </tr>
        </thead>
        <tbody>
        <% for(let item of watch){ %>
            <tr>
                <td><%= item["name"] %></td>
                <td><%= item["year"] %></td>
                <td><%= item["platforms"].join(', ') %></td>
                <td><%= item["genres"].join(', ') %></td>
            </tr>
        <% } %>
        </tbody>
    </table>
</div>
<br/>
<br/>
<div class="container">
    <input type="text" id="search2" class="search-box" placeholder="Search In Played Games..."
           onkeyup="search('search2', '<%= header %>2')"/>
</div>
<br/>
<div class="container">
    <table id="<%= header %>2" class="table table-bordered">
        <colgroup>
            <col class="small"/>
            <col class="small"/>
            <col class="medium"/>
            <col class="medium"/>
        </colgroup>
        <thead>
        <tr style="background-color: #9147ff">
            <th>Name</th>
            <th>Year</th>
            <th>Platform(s)</th>
            <th>Genre(s)</th>
        </tr>
        </thead>
        <tbody>
        <% for(let item of play){ %>
            <tr>
                <td><%= item["name"] %></td>
                <td><%= item["year"] %></td>
                <td><%= item["platforms"].join(', ') %></td>
                <td><%= item["genres"].join(', ') %></td>
            </tr>
        <% } %>
        </tbody>
    </table>
</div>
<br/>